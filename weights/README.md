# Checkpoints

Place MAR model checkpoints here or edit `configs/default.yaml` to point to
your local checkpoint paths.

The cleaned repository intentionally does not commit `.pt`, `.pth`, `.ckpt`, or
`.onnx` files.
